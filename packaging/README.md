# Packaging

This stage teaches how a Python repository becomes a reusable package.

Focus on:

- package layout
- entry points
- running packages with `python -m`
- install thinking

Suggested order:

1. `01_local_package_layout.py`
2. `02_entry_points_overview.py`
