# Challenge Backlog

Use this backlog when you want extra practice without needing new lessons.

## Foundations Challenges

- Rewrite `05_loops.py` using different control flow.
- Turn `06_functions.py` into a mini calculator.
- Add your own custom exception to `10_exceptions.py`.

## Core Python Challenges

- Add another decorator to `04_decorators_intro.py`.
- Extend `05_dataclasses.py` with one computed property.
- Add a second protocol example to `08_typing_protocols.py`.

## Standard Library Challenges

- Extend `05_sqlite_basics.py` with an update query.
- Change `06_csv_basics.py` to calculate a total.
- Add another regex extraction case in `04_regular_expressions.py`.

## Engineering Challenges

- Move one `assert` example into a `unittest` test case.
- Add environment-driven log levels to `06_logging_basics.py`.
- Add one more retry rule to `09_retry_pattern.py`.

## Advanced Challenges

- Compare cached and non-cached Fibonacci.
- Add a second worker to `09_queue_worker.py`.
- Explain when `threading` helps and when it does not.

## Projects Challenges

- Add delete support to `01_todo_cli.py`.
- Add summary stats to `03_study_tracker_sqlite.py`.
- Add update support to `04_habit_tracker_app/`.
- Add another report field to `05_study_assistant_app/`.
- Add topic history to `06_revision_trainer_app/`.

## Interview Challenges

- Solve two-sum in a second way.
- Write an anagram checker.
- Write a character frequency counter.
